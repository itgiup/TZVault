# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
src-tauri/target/

# Vault data - PHÒNG HỜ. Về mặt thiết kế, vault.db không bao giờ nằm
# trong repo (lưu ở app_data_dir của OS, xem main.rs), nhưng thêm dòng
# này để chặn tuyệt đối trường hợp ai đó lỡ tạo file test ngay trong
# thư mục project.
*.db
*.db-wal
*.db-shm
app_config.json

# Env / secrets
.env
.env.*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Tauri generated (tự sinh lại lúc build, không cần commit)
src-tauri/gen/